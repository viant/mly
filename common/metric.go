package common


const (
	//MetricURI represents metric URI
	MetricURI = "/v1/api/metric/"
	//MetricEndpointCounter endpoint metric
	MetricEndpointCounter = "endpoint"
	//EvaluatorFilterCounter metric
	EvaluatorFilterCounter = "eval"
)
